import 'package:flutter/material.dart';

class AppBorderRadius {
  AppBorderRadius._();

  static const BorderRadius xsmall = BorderRadius.all(Radius.circular(4));
  static const BorderRadius small = BorderRadius.all(Radius.circular(8));
  static const BorderRadius base = BorderRadius.all(Radius.circular(12));
  static const BorderRadius medium = BorderRadius.all(Radius.circular(16));
  static const BorderRadius large = BorderRadius.all(Radius.circular(24));
  static const BorderRadius xlarge = BorderRadius.all(Radius.circular(32));

  static const BorderRadius xs = xsmall;
  static const BorderRadius sm = small;
  static const BorderRadius md = medium;
  static const BorderRadius lg = large;
  static const BorderRadius xl = xlarge;
}
